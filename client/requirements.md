## Packages
date-fns | Date manipulation utilities
framer-motion | Smooth animations and enter/exit transitions

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
Dark mode is forced at the CSS root level to ensure a consistent, premium Notion-like aesthetic.
